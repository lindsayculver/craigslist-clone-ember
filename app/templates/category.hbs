<h1>{{model.name}}</h1>

{{new-listing saveListing="saveListing" category=model}}

{{#each model.listings as |listing|}}
{{#link-to 'listing' listing.id}}
  {{listing.title}}
  <p>for {{listing.price}} from {{listing.seller}}</p>
{{/link-to}}
{{/each}}



{{outlet}}
